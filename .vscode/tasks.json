{
    "version" : "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake -S . -B ${command:cmake.buildDirectory}; cmake --build ${command:cmake.buildDirectory}",
        }
    ]
}
